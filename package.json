{
  "name": "aibtcdev-smart-contracts",
  "version": "0.0.2",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "generate-types": "npx clarigen",
    "test": "vitest run --reporter verbose",
    "test:report": "vitest run --reporter verbose -- --coverage --costs",
    "test:watch": "chokidar \"tests/**/*.ts\" \"contracts/**/*.clar\" -c \"npm run test:report\""
  },
  "dependencies": {
    "@clarigen/cli": "^2.0.6",
    "@hirosystems/clarinet-sdk": "^2.3.2",
    "@stacks/transactions": "^6.12.0",
    "chokidar-cli": "^3.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vitest": "^1.3.1",
    "vitest-environment-clarinet": "^2.0.0"
  }
}
